export default function ComparisonPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold text-slate-900">Vendor comparison</h1>
      <p className="mt-2 text-sm text-slate-500">
        Side-by-side line items x vendors grid with confidence indicators, source citations, and
        questionnaire answers (modules 3 &amp; 4). Not wired up yet.
      </p>
    </div>
  )
}
