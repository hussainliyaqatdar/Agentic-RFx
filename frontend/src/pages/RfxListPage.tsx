export default function RfxListPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold text-slate-900">RFx list</h1>
      <p className="mt-2 text-sm text-slate-500">
        Draft, sent, and awarded RFx go here, each with a status and a link into its detail page.
        Not wired up yet — this is the module 2 review/approval list from the scope doc.
      </p>
    </div>
  )
}
