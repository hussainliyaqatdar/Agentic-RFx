# Backend origin for a production build, no trailing slash (e.g. https://agentic-rfx.onrender.com).
# Leave unset for local dev - Vite's dev server proxies /api to the local backend instead.
VITE_API_URL=
